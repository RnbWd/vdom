import { Attributes } from './types'
import { VirtualNode } from './tree'

export const update = (root: VirtualNode) => (vnode: VirtualNode) => {}

export const add = (root: VirtualNode) => (vnode: VirtualNode) => {}

export const remove = (root: VirtualNode) => (vnode: VirtualNode) => {}
